<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <link rel="shortcut icon" href="/favicon.ico"><link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png"><link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png"><link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png"><meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Marketing</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin-modern.css">
  <style>
    :root{--cor-primaria:<%= config.org_cor||'#1a56db' %>;--rad:2px;--card:#ffffff;--borda:#e2e8f0;--texto2:#64748b;}
    .mkt-tabs{display:flex;gap:6px;margin-bottom:20px;flex-wrap:wrap;background:var(--card);border:1px solid var(--borda);border-radius:var(--rad);padding:6px;}
    .mkt-tab{display:inline-flex;align-items:center;gap:6px;padding:8px 16px;border-radius:var(--rad);border:none;background:transparent;cursor:pointer;font-weight:600;font-size:13px;transition:all .2s;color:var(--texto2);font-family:inherit;}
    .mkt-tab:hover{background:var(--fundo);color:var(--texto);}
    .mkt-tab.ativo{background:var(--cor-primaria);color:white;}
  </style>
  <script>
    function showTab(id,btn){
      document.querySelectorAll('.mkt-section').forEach(s=>s.classList.remove('ativo'));
      document.querySelectorAll('.mkt-tab').forEach(t=>t.classList.remove('ativo'));
      var sec=document.getElementById('tab-'+id);
      if(sec) sec.classList.add('ativo');
      if(btn) btn.classList.add('ativo');
      if(id==='integracao' && typeof carregarIgFeed==='function'){
        carregarIgFeed();
        if(typeof carregarIgPerfil==='function') carregarIgPerfil();
      }
      if(id==='midias' && typeof canvaCarregarDesigns==='function') canvaCarregarDesigns();
    }
    document.addEventListener('DOMContentLoaded',function(){
      var tab=new URLSearchParams(window.location.search).get('tab');
      if(tab){
        var btn=document.querySelector('.mkt-tab[onclick*="\''+tab+'\'"]');
        if(btn) showTab(tab,btn);
      }
    });
  </script>
  <style>
    .mkt-section{display:none!important;}
    .mkt-section.ativo{display:block!important;}
    .stats-row{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;margin-bottom:20px;}
    .stat-box{background:var(--card);border:1px solid var(--borda);border-radius:var(--rad);padding:16px;position:relative;overflow:hidden;}
    .stat-box::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:var(--cor-stat,var(--cor-primaria));}
    .stat-num{font-size:24px;font-weight:700;line-height:1;margin-bottom:4px;color:var(--cor-stat,var(--cor-primaria));}
    .stat-lab{font-size:12px;color:var(--texto2);font-weight:600;}
    .post-card{background:var(--card);border:1px solid var(--borda);border-radius:var(--rad);padding:16px;margin-bottom:10px;transition:box-shadow .15s;}
    .post-card:hover{box-shadow:0 2px 12px rgba(0,0,0,.06);}
    .rede-badge{padding:3px 8px;border-radius:var(--rad);font-size:11px;font-weight:700;display:inline-flex;align-items:center;gap:4px;}
    .rede-instagram{background:#e1306c;color:white;}
    .rede-facebook{background:#1877f2;color:white;}
    .rede-whatsapp{background:#25d366;color:white;}
    .badge-status{display:inline-flex;align-items:center;padding:3px 10px;border-radius:var(--rad);font-size:11px;font-weight:700;}
    .status-rascunho{background:#f3f4f6;color:#6b7280;}
    .status-agendado{background:#dbeafe;color:#1e40af;}
    .status-publicado{background:#dcfce7;color:#166534;}
    .status-erro{background:#fee2e2;color:#991b1b;}
    .calendario-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;}
    .cal-header{text-align:center;font-weight:700;font-size:11px;padding:6px;color:var(--texto2);text-transform:uppercase;}
    .cal-dia{min-height:72px;background:var(--card);border:1px solid var(--borda);border-radius:var(--rad);padding:6px;transition:.15s;}
    .cal-dia.hoje{border-color:var(--cor-primaria);background:#eff6ff;}
    .cal-dia.outro-mes{opacity:.35;}
    .cal-num{font-size:12px;font-weight:700;margin-bottom:3px;}
    .cal-post{background:var(--cor-primaria);color:white;border-radius:var(--rad);padding:2px 5px;font-size:10px;margin-bottom:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;cursor:pointer;}
    .midia-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:12px;}
    .midia-card{border:1px solid var(--borda);border-radius:var(--rad);overflow:hidden;position:relative;cursor:pointer;transition:.15s;}
    .midia-card:hover{border-color:var(--cor-primaria);transform:translateY(-2px);}
    .midia-card img{width:100%;height:120px;object-fit:cover;display:block;}
    .midia-overlay{position:absolute;inset:0;background:rgba(0,0,0,.5);opacity:0;transition:.2s;display:flex;align-items:center;justify-content:center;gap:8px;}
    .midia-card:hover .midia-overlay{opacity:1;}
    .midia-card-info{padding:6px 8px;font-size:11px;font-weight:600;}
    .integ-card{background:var(--card);border:1px solid var(--borda);border-radius:var(--rad);padding:20px;}
    .integ-logo{width:44px;height:44px;border-radius:var(--rad);display:flex;align-items:center;justify-content:center;flex-shrink:0;}
    .badge-conectado{background:#dcfce7;color:#166534;padding:3px 10px;border-radius:var(--rad);font-size:11px;font-weight:700;}
    .badge-desconectado{background:#fee2e2;color:#991b1b;padding:3px 10px;border-radius:var(--rad);font-size:11px;font-weight:700;}
    .whatsapp-preview{background:#e5ddd5;border-radius:var(--rad);padding:16px;min-height:200px;}
    .wpp-bubble{background:white;border-radius:var(--rad);padding:10px 14px;margin-bottom:8px;box-shadow:0 1px 2px rgba(0,0,0,.1);font-size:13px;max-width:85%;}
    .wpp-bubble.saida{background:#d9fdd3;margin-left:auto;}
    .engaj-bar{display:flex;align-items:center;gap:10px;margin-bottom:10px;}
    .engaj-label{width:90px;font-size:12px;color:var(--texto2);font-weight:600;}
    .engaj-fill{flex:1;background:var(--fundo);border-radius:var(--rad);height:8px;overflow:hidden;border:1px solid var(--borda);}
    .engaj-valor{width:36px;font-size:12px;font-weight:700;text-align:right;}
    .btn-xs{display:inline-flex;align-items:center;gap:4px;padding:5px 10px;border-radius:var(--rad);font-size:11px;font-weight:600;border:1px solid var(--borda);background:var(--fundo);color:var(--texto);cursor:pointer;text-decoration:none;transition:all .15s;font-family:inherit;white-space:nowrap;}
    .btn-xs:hover{border-color:var(--cor-primaria);color:var(--cor-primaria);}
    .btn-xs.verde{background:#059669;color:white;border-color:transparent;}
    .btn-xs.roxo{background:#7c3aed;color:white;border-color:transparent;}
    .btn-xs.verm{color:#dc2626;border-color:#fecaca;}
    .btn-xs.azul{background:#1d4ed8;color:white;border-color:transparent;}
    .ia-loading{display:none;align-items:center;gap:8px;font-size:13px;color:var(--texto2);padding:8px;}
    .ia-loading.ativo{display:flex;}
    .dots span{animation:dots 1.2s infinite;opacity:0;}
    .dots span:nth-child(2){animation-delay:.2s;}
    .dots span:nth-child(3){animation-delay:.4s;}
    @keyframes dots{0%,80%,100%{opacity:0}40%{opacity:1}}
    .template-btn{display:block;width:100%;text-align:left;padding:10px 14px;border:1px solid var(--borda);border-radius:var(--rad);background:var(--fundo);cursor:pointer;font-size:12px;margin-bottom:6px;transition:.15s;font-family:inherit;}
    .template-btn:hover{border-color:var(--cor-primaria);background:#eff6ff;color:var(--cor-primaria);}
    .search-bar{display:flex;align-items:center;gap:8px;background:var(--fundo);border:1px solid var(--borda);border-radius:var(--rad);padding:0 12px;height:36px;}
    .search-bar input{border:none;background:transparent;font-size:13px;color:var(--texto);outline:none;width:180px;font-family:inherit;}
    .filter-pills{display:flex;gap:6px;flex-wrap:wrap;}
    .pill{padding:5px 14px;border-radius:var(--rad);border:1.5px solid var(--borda);background:var(--card);font-size:12px;font-weight:600;cursor:pointer;transition:.15s;color:var(--texto2);}
    .pill.ativo{background:var(--cor-primaria);color:white;border-color:var(--cor-primaria);}
    .score-wrap{background:var(--fundo);border:1px solid var(--borda);border-radius:var(--rad);padding:12px 14px;margin-top:8px;}
    .score-bar-wrap{height:8px;border-radius:var(--rad);background:#e5e7eb;overflow:hidden;margin:6px 0;}
    .score-bar-fill{height:100%;border-radius:var(--rad);transition:width .4s;}
    .score-label{font-size:11px;font-weight:700;display:flex;justify-content:space-between;}
    .hashtag-grid{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px;}
    .hashtag-chip{padding:3px 10px;border-radius:var(--rad);background:#eff6ff;color:var(--cor-primaria);font-size:11px;font-weight:600;cursor:pointer;border:1.5px solid #bfdbfe;transition:.15s;}
    .hashtag-chip:hover{background:var(--cor-primaria);color:white;border-color:var(--cor-primaria);}
    .hashtag-chip.selecionado{background:var(--cor-primaria);color:white;border-color:var(--cor-primaria);}
    .horario-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;margin-top:8px;}
    .horario-dia{text-align:center;font-size:10px;}
    .horario-dia-nome{font-weight:700;color:var(--texto2);margin-bottom:4px;}
    .horario-slot{padding:3px 2px;border-radius:var(--rad);font-size:9px;font-weight:600;text-align:center;margin-bottom:2px;}
    .slot-bom{background:#dcfce7;color:#166534;}
    .slot-otimo{background:#059669;color:white;}
    .slot-fraco{background:#f3f4f6;color:#9ca3af;}
    .aprovacao-badge{display:inline-flex;align-items:center;gap:5px;padding:3px 10px;border-radius:var(--rad);font-size:11px;font-weight:700;}
    .aprovacao-pendente{background:#fef3c7;color:#92400e;}
    .aprovacao-aprovado{background:#dcfce7;color:#166534;}
    .aprovacao-rejeitado{background:#fee2e2;color:#991b1b;}
    .historico-item{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid var(--borda);}
    .historico-item:last-child{border-bottom:none;}
    .preview-social{border:1px solid var(--borda);border-radius:var(--rad);overflow:hidden;background:white;}
    .preview-social-header{padding:10px 12px;display:flex;align-items:center;gap:8px;border-bottom:1px solid #f0f0f0;}
    .preview-social-img{width:36px;height:36px;border-radius:50%;background:linear-gradient(45deg,#f09433,#e6683c,#dc2743,#cc2366,#bc1888);}
    .preview-social-body{padding:10px 12px;font-size:13px;line-height:1.5;}
    .preview-social-footer{padding:6px 12px;font-size:11px;color:#9ca3af;border-top:1px solid #f0f0f0;display:flex;gap:12px;}
    .campanha-step{display:flex;align-items:center;gap:10px;padding:10px;border:1px solid var(--borda);border-radius:var(--rad);margin-bottom:8px;}
    .campanha-step-num{width:28px;height:28px;border-radius:50%;background:var(--cor-primaria);color:white;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;flex-shrink:0;}
  
      .stat-box{transition:transform .18s,border-color .18s,box-shadow .18s;}
      .stat-box:hover{transform:translateY(-3px);box-shadow:0 6px 18px rgba(0,0,0,.08);border-color:var(--cor-stat,var(--cor-primaria));}
      .post-card{transition:box-shadow .15s,transform .15s;}
      .post-card:hover{box-shadow:0 4px 16px rgba(0,0,0,.08);transform:translateY(-1px);}
      .btn-xs:hover{transform:translateY(-1px);}
      .pill:hover{transform:translateY(-1px);}
      .mkt-tab:hover{transform:translateY(-1px);}
    </style>
</head>
<body>
<div class="layout">
  <%- include('../partials/sidebar', { paginaAtual: 'marketing' }) %>
  <main class="main">
    <div class="topbar">
      <h1 class="page-titulo">Marketing</h1>
      <div class="topbar-acoes">
        <button class="btn btn-sm" onclick="abrirModal('modal-wpp-massa')">WhatsApp em Massa</button>
        <button class="btn btn-sm btn-primario" onclick="abrirModal('modal-novo-post')">+ Novo Post</button>
      </div>
    </div>
    <div class="content">
      <%if(msg&&msg.length>0){%><div class="alerta alerta-ok"><%=msg[0]%></div><%}%>
      <%if(erro&&erro.length>0){%><div class="alerta alerta-erro"><%=erro[0]%></div><%}%>
      <div class="mkt-tabs">
        <button class="mkt-tab ativo" onclick="showTab('dashboard',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/></svg>
          Dashboard</button>
        <button class="mkt-tab" onclick="showTab('posts',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
          Posts</button>
        <button class="mkt-tab" onclick="showTab('calendario',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
          Calendario</button>
        <button class="mkt-tab" onclick="showTab('midias',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
          Midias</button>
        <button class="mkt-tab" onclick="showTab('equipe',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          Site LAURO</button>
        <button class="mkt-tab" onclick="showTab('whatsapp',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
          WhatsApp</button>
        <button class="mkt-tab" onclick="showTab('integracao',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="18" cy="18" r="3"/><circle cx="6" cy="6" r="3"/><path d="M13 6h3a2 2 0 0 1 2 2v7"/><line x1="6" y1="9" x2="6" y2="21"/></svg>
          Integracoes</button>
        <button class="mkt-tab" onclick="showTab('aprovacao',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          Aprovacoes</button>
        <button class="mkt-tab" onclick="showTab('campanha',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
          Campanhas</button>
        <button class="mkt-tab" onclick="showTab('templates',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="9" y1="21" x2="9" y2="9"/></svg>
          Templates</button>
        <button class="mkt-tab" onclick="showTab('historico',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
          Historico</button>
        <button class="mkt-tab" onclick="showTab('analytics',this)">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
          Analytics</button>
      </div>
      <div id="mkt-content">
      <div class="mkt-section ativo" id="tab-dashboard">
        <%const total=posts.length;const agend=posts.filter(p=>p.status==='agendado').length;
        const pub=posts.filter(p=>p.status==='publicado').length;
        const rasc=posts.filter(p=>p.status==='rascunho').length;%>
        <div class="stats-row">
          <div class="stat-box" style="--cor-stat:var(--cor-primaria)">
            <div class="stat-num"><%=total%></div><div class="stat-lab">Total de Posts</div>
          </div>
          <div class="stat-box" style="--cor-stat:#f59e0b">
            <div class="stat-num"><%=agend%></div><div class="stat-lab">Agendados</div>
          </div>
          <div class="stat-box" style="--cor-stat:#10b981">
            <div class="stat-num"><%=pub%></div><div class="stat-lab">Publicados</div>
          </div>
          <div class="stat-box" style="--cor-stat:#6b7280">
            <div class="stat-num"><%=rasc%></div><div class="stat-lab">Rascunhos</div>
          </div>
          <div class="stat-box" style="--cor-stat:#0891b2">
            <div class="stat-num"><%=midias.length%></div><div class="stat-lab">Midias</div>
          </div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Proximos Posts Agendados</div>
            <%const prox=posts.filter(p=>p.status==='agendado').slice(0,5);%>
            <%if(prox.length===0){%>
            <p style="color:var(--texto2);font-size:13px">Nenhum post agendado</p>
            <%}%>
            <%prox.forEach(p=>{%>
            <div class="post-card" style="padding:10px;margin-bottom:8px">
              <div style="font-weight:600;font-size:13px"><%=p.titulo%></div>
              <div style="font-size:11px;color:var(--texto2);margin-top:3px">
                <%=p.agendado_para?new Date(p.agendado_para).toLocaleString('pt-BR'):'&#8212;'%>
              </div>
              <div style="margin-top:6px;display:flex;gap:4px">
                <%(p.redes||[]).forEach(r=>{%>
                <span class="rede-badge rede-<%=r%>"><%=r%></span>
                <%});%>
              </div>
            </div>
            <%});%>
          </div>
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Distribuicao por Rede</div>
            <div class="engaj-bar">
              <div class="engaj-label">Instagram</div>
              <div class="engaj-fill"><div style="height:100%;background:#e1306c;width:<%=igPct%>%"></div></div>
              <div class="engaj-valor" style="color:#e1306c"><%=posts.filter(p=>(p.redes||[]).includes('instagram')).length%></div>
            </div>
            <div class="engaj-bar">
              <div class="engaj-label">Facebook</div>
              <div class="engaj-fill"><div style="height:100%;background:#1877f2;width:<%=fbPct%>%"></div></div>
              <div class="engaj-valor" style="color:#1877f2"><%=posts.filter(p=>(p.redes||[]).includes('facebook')).length%></div>
            </div>
            <div class="engaj-bar">
              <div class="engaj-label">WhatsApp</div>
              <div class="engaj-fill"><div style="height:100%;background:#25d366;width:<%=waPct%>%"></div></div>
              <div class="engaj-valor" style="color:#25d366"><%=posts.filter(p=>(p.redes||[]).includes('whatsapp')).length%></div>
            </div>
          </div>
        </div>
        <div class="card" style="margin-top:16px">
          <div class="card-titulo" style="margin-bottom:14px">Melhores Horarios para Postar</div>
          <div style="font-size:12px;color:var(--texto2);margin-bottom:10px">Baseado em dados do setor de saude e educacao no Brasil</div>
          <div class="horario-grid">
            <div class="horario-dia"><div class="horario-dia-nome">Dom</div><div class="horario-slot slot-fraco">8h</div><div class="horario-slot slot-fraco">12h</div><div class="horario-slot slot-bom">18h</div><div class="horario-slot slot-fraco">21h</div></div>
            <div class="horario-dia"><div class="horario-dia-nome">Seg</div><div class="horario-slot slot-bom">8h</div><div class="horario-slot slot-otimo">12h</div><div class="horario-slot slot-bom">18h</div><div class="horario-slot slot-bom">20h</div></div>
            <div class="horario-dia"><div class="horario-dia-nome">Ter</div><div class="horario-slot slot-bom">8h</div><div class="horario-slot slot-otimo">12h</div><div class="horario-slot slot-otimo">18h</div><div class="horario-slot slot-bom">20h</div></div>
            <div class="horario-dia"><div class="horario-dia-nome">Qua</div><div class="horario-slot slot-otimo">8h</div><div class="horario-slot slot-otimo">12h</div><div class="horario-slot slot-otimo">18h</div><div class="horario-slot slot-bom">20h</div></div>
            <div class="horario-dia"><div class="horario-dia-nome">Qui</div><div class="horario-slot slot-bom">8h</div><div class="horario-slot slot-otimo">12h</div><div class="horario-slot slot-bom">18h</div><div class="horario-slot slot-bom">20h</div></div>
            <div class="horario-dia"><div class="horario-dia-nome">Sex</div><div class="horario-slot slot-bom">8h</div><div class="horario-slot slot-bom">12h</div><div class="horario-slot slot-fraco">18h</div><div class="horario-slot slot-fraco">20h</div></div>
            <div class="horario-dia"><div class="horario-dia-nome">Sab</div><div class="horario-slot slot-fraco">10h</div><div class="horario-slot slot-bom">12h</div><div class="horario-slot slot-bom">16h</div><div class="horario-slot slot-fraco">20h</div></div>
          </div>

        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:12px;margin-bottom:16px">
          <div class="filter-pills">
            <span class="pill ativo" onclick="filtrarPosts('todos',this)">Todos (<%=total%>)</span>
            <span class="pill" onclick="filtrarPosts('rascunho',this)">Rascunhos</span>
            <span class="pill" onclick="filtrarPosts('agendado',this)">Agendados</span>
            <span class="pill" onclick="filtrarPosts('publicado',this)">Publicados</span>
          </div>
          <div class="search-bar">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="var(--texto2)"><path d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z"/></svg>
            <input type="text" placeholder="Buscar post..." oninput="buscarPost(this.value)">
          </div>
        </div>
        <%if(posts.length===0){%>
        <div style="text-align:center;padding:48px;color:var(--texto2)">
          <div style="margin-bottom:12px;display:flex;justify-content:center;opacity:.35"><svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg></div>
          <div style="font-weight:600">Nenhum post criado ainda</div>
        </div><%}%>
        <%posts.forEach(p=>{%>
        <div class="post-card post-item" data-status="<%=p.status%>" data-titulo="<%=(p.titulo||'').toLowerCase()%>">
          <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:12px">
            <div style="display:flex;align-items:center;gap:12px;flex:1">
              <%if(p.imagem_chave){%>
              <img src="/marketing/midia/<%=p.id%>/img" style="width:52px;height:52px;border-radius:var(--rad);object-fit:cover;flex-shrink:0">
              <%}else{%>
              <div style="width:52px;height:52px;border-radius:var(--rad);background:var(--fundo);display:flex;align-items:center;justify-content:center;flex-shrink:0;border:1px solid var(--borda)">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--texto2)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
              </div><%}%>
              <div style="flex:1;min-width:0">
                <div style="font-weight:700;font-size:14px;margin-bottom:3px"><%=p.titulo||'(Sem titulo)'%></div>
                <div style="font-size:12px;color:var(--texto2)"><%=(p.conteudo||'').substring(0,100)%><%=(p.conteudo||'').length>100?'...':''%></div>
              </div>
            </div>
            <span class="badge-status status-<%=p.status%>"><%=p.status%></span>
          </div>
          <div style="display:flex;justify-content:space-between;align-items:center;margin-top:12px;flex-wrap:wrap;gap:8px">
            <div style="display:flex;gap:4px;flex-wrap:wrap">
              <%(p.redes||[]).forEach(r=>{%>
              <span class="rede-badge rede-<%=r%>"><%=r%></span>
              <%});%>
              <%if(p.agendado_para){%>
              <span style="font-size:11px;color:var(--texto2);margin-left:6px">
                <%=new Date(p.agendado_para).toLocaleString('pt-BR')%>
              </span><%}%>
            </div>
            <div style="display:flex;gap:5px">
              <button onclick="editarPost(<%=p.id%>)" class="btn-xs">Editar</button>
              <%if(p.status==='rascunho'||p.status==='agendado'){%>
              <form method="POST" action="/marketing/<%=p.id%>/publicar" style="display:inline;margin:0">
                <button type="submit" class="btn-xs verde">Publicar</button></form>
              <%}%>
              <form method="POST" action="/marketing/<%=p.id%>/deletar" style="display:inline;margin:0" onsubmit="var f=this; confirmarAcao('Excluir?', function(){ f.submit(); }); return false;">
                <button type="submit" class="btn-xs verm">Excluir</button></form>
            </div>
          </div>
        </div>
        <%});%>
      </div>
      </div>
      <div class="mkt-section" id="tab-posts">
        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:12px;margin-bottom:16px">
          <div class="filter-pills">
            <span class="pill ativo" onclick="filtrarPosts('todos',this)">Todos (<%=posts.length%>)</span>
            <span class="pill" onclick="filtrarPosts('rascunho',this)">Rascunhos</span>
            <span class="pill" onclick="filtrarPosts('agendado',this)">Agendados</span>
            <span class="pill" onclick="filtrarPosts('publicado',this)">Publicados</span>
          </div>
          <div class="search-bar">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="var(--texto2)"><path d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z"/></svg>
            <input type="text" placeholder="Buscar post..." oninput="buscarPost(this.value)">
          </div>
        </div>
        <%if(posts.length===0){%>
        <div style="text-align:center;padding:48px;color:var(--texto2)">
          <div style="margin-bottom:12px;display:flex;justify-content:center;opacity:.35"><svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg></div>
          <div style="font-weight:600">Nenhum post criado ainda</div>
        </div><%}%>
        <%posts.forEach(p=>{%>
        <div class="post-card post-item" data-status="<%=p.status%>" data-titulo="<%=(p.titulo||'').toLowerCase()%>">
          <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:12px">
            <div style="display:flex;align-items:center;gap:12px;flex:1">
              <%if(p.imagem_chave){%>
              <img src="/marketing/midia/<%=p.id%>/img" style="width:52px;height:52px;border-radius:var(--rad);object-fit:cover;flex-shrink:0">
              <%}else{%>
              <div style="width:52px;height:52px;border-radius:var(--rad);background:var(--fundo);display:flex;align-items:center;justify-content:center;flex-shrink:0;border:1px solid var(--borda)">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--texto2)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
              </div><%}%>
              <div style="flex:1;min-width:0">
                <div style="font-weight:700;font-size:14px;margin-bottom:3px"><%=p.titulo||'(Sem titulo)'%></div>
                <div style="font-size:12px;color:var(--texto2)"><%=(p.conteudo||'').substring(0,100)%><%=(p.conteudo||'').length>100?'...':''%></div>
              </div>
            </div>
            <span class="badge-status status-<%=p.status%>"><%=p.status%></span>
          </div>
          <div style="display:flex;justify-content:space-between;align-items:center;margin-top:12px;flex-wrap:wrap;gap:8px">
            <div style="display:flex;gap:4px;flex-wrap:wrap">
              <%(p.redes||[]).forEach(r=>{%><span class="rede-badge rede-<%=r%>"><%=r%></span><%});%>
              <%if(p.agendado_para){%><span style="font-size:11px;color:var(--texto2);margin-left:6px"><%=new Date(p.agendado_para).toLocaleString('pt-BR')%></span><%}%>
            </div>
            <div style="display:flex;gap:5px">
              <button onclick="editarPost(<%=p.id%>)" class="btn-xs">Editar</button>
              <%if(p.status==='rascunho'||p.status==='agendado'){%>
              <form method="POST" action="/marketing/<%=p.id%>/publicar" style="display:inline;margin:0"><button type="submit" class="btn-xs verde">Publicar</button></form>
              <%}%>
              <form method="POST" action="/marketing/<%=p.id%>/deletar" style="display:inline;margin:0" onsubmit="var f=this; confirmarAcao('Excluir?', function(){ f.submit(); }); return false;">
                <button type="submit" class="btn-xs verm">Excluir</button></form>
            </div>
          </div>
        </div>
        <%});%>
      </div>
      <div class="mkt-section" id="tab-calendario">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px">
          <button class="btn btn-sm" onclick="mudarMes(-1)">&#8592; Anterior</button>
          <h3 id="cal-titulo" style="font-size:16px;font-weight:700;font-family:'Outfit',sans-serif"></h3>
          <button class="btn btn-sm" onclick="mudarMes(1)">Proximo &#8594;</button>
        </div>
        <div class="calendario-grid">
          <div class="cal-header">Dom</div><div class="cal-header">Seg</div><div class="cal-header">Ter</div>
          <div class="cal-header">Qua</div><div class="cal-header">Qui</div><div class="cal-header">Sex</div><div class="cal-header">Sab</div>
        </div>
        <div class="calendario-grid" id="cal-grid" style="margin-top:4px"></div>
      </div>
      <div class="mkt-section" id="tab-midias">
        <%if(typeof canvaConectado!=='undefined' && canvaConectado){%>
        <div class="card" style="margin-bottom:20px">
          <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;flex-wrap:wrap;gap:8px">
            <div class="card-titulo" style="margin:0">Canva</div>
            <div style="display:flex;gap:8px">
              <select id="canva-tipo-novo" class="input" style="width:auto" onchange="canvaTogglePersonalizado(this.value)">
                <optgroup label="Instagram">
                  <option value="InstagramPost">Post (1080x1080)</option>
                  <option value="InstagramPortrait">Post vertical (1080x1350)</option>
                  <option value="InstagramStory">Story (1080x1920)</option>
                  <option value="InstagramReel">Reel (1080x1920)</option>
                </optgroup>
                <optgroup label="Facebook">
                  <option value="FacebookPost">Post (1200x630)</option>
                  <option value="FacebookCover">Capa (820x312)</option>
                  <option value="FacebookStory">Story (1080x1920)</option>
                </optgroup>
                <optgroup label="Outras redes">
                  <option value="TwitterPost">Post X/Twitter (1600x900)</option>
                  <option value="LinkedinPost">Post LinkedIn (1200x627)</option>
                  <option value="WhatsappStatus">Status WhatsApp (1080x1920)</option>
                  <option value="YoutubeThumbnail">Thumbnail YouTube (1280x720)</option>
                  <option value="YoutubeBanner">Banner YouTube (2560x1440)</option>
                </optgroup>
                <optgroup label="Impressos e documentos">
                  <option value="Poster">Cartaz (1080x1350)</option>
                  <option value="Flyer">Panfleto A4 (1240x1754)</option>
                  <option value="Presentation">Apresentacao</option>
                  <option value="Doc">Documento</option>
                  <option value="Whiteboard">Quadro branco</option>
                </optgroup>
                <optgroup label="Outro">
                  <option value="Personalizado">Personalizado (definir tamanho)</option>
                </optgroup>
              </select>
              <input type="number" id="canva-largura" class="input" placeholder="Largura (px)" style="width:110px;display:none">
              <input type="number" id="canva-altura" class="input" placeholder="Altura (px)" style="width:110px;display:none">
              <button class="btn btn-primario btn-sm" onclick="canvaCriarDesign()">+ Criar novo design</button>
              <button class="btn btn-sm" onclick="canvaCarregarDesigns()">Atualizar</button>
            </div>
          </div>
          <div id="canva-designs-grid" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:12px">
            <div style="grid-column:1/-1;text-align:center;padding:20px;color:var(--texto2)">Carregando designs do Canva...</div>
          </div>
        </div>
        <%}%>
        <div style="margin-bottom:16px">
          <form action="/marketing/midias/upload" method="POST" enctype="multipart/form-data" style="display:flex;gap:8px;align-items:center;flex-wrap:wrap">
            <input type="file" name="midia" class="input" accept="image/*,video/*" style="flex:1;min-width:200px">
            <input type="text" name="nome" class="input" placeholder="Nome da midia" style="flex:1;min-width:150px">
            <button type="submit" class="btn btn-primario btn-sm">Upload</button>
          </form>
        </div>
        <%if(midias.length===0){%><div style="text-align:center;padding:48px;color:var(--texto2)"><div style="margin-bottom:8px;display:flex;justify-content:center;opacity:.35"><svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg></div><div style="font-weight:600">Nenhuma midia enviada</div></div><%}%>
        <div class="midia-grid">
          <%midias.forEach(m=>{%>
          <div class="midia-card">
            <img src="/marketing/midias/<%=m.id%>/img" alt="<%=m.nome%>" onerror="this.onerror=null;this.src='/img/placeholder.png'">
            <div class="midia-overlay">
              <button onclick="usarMidia('<%=m.id%>','<%=m.nome%>')" class="btn-xs" style="background:white;color:#111">Usar</button>
              <form method="POST" action="/marketing/midias/<%=m.id%>/deletar" style="display:inline;margin:0" onsubmit="var f=this; confirmarAcao('Excluir?', function(){ f.submit(); }); return false;">
                <button type="submit" class="btn-xs verm" style="background:#dc2626;color:white;border-color:transparent">Excluir</button></form>
            </div>
            <div class="midia-card-info"><%=m.nome%></div>
          </div>
          <%});%>
        </div>
      </div>
      <div class="mkt-section" id="tab-equipe">
        <style>
          .equipe-aviso{background:#eff6ff;border:1px solid #bfdbfe;color:#1e40af;padding:12px 16px;margin-bottom:20px;font-size:13px;border-radius:var(--rad)}
          .equipe-bloco{background:var(--card);border:1px solid var(--borda);border-radius:var(--rad);padding:18px;margin-bottom:20px}
          .equipe-bloco-header{display:flex;align-items:center;gap:10px;margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid var(--borda)}
          .equipe-bloco-icone{width:34px;height:34px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;color:white}
          .equipe-bloco-titulo{font-size:14px;font-weight:700;color:var(--texto)}
          .equipe-bloco-contagem{font-size:12px;color:var(--texto2);margin-left:auto;background:var(--fundo);padding:3px 10px;border-radius:20px;font-weight:600}
          .equipe-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:14px}
          .equipe-card{border:1px solid var(--borda);border-radius:var(--rad);overflow:hidden;background:var(--fundo)}
          .equipe-card img{width:100%;height:130px;object-fit:cover;display:block;background:#e2e8f0}
          .equipe-card-corpo{padding:10px}
          .equipe-card-nome{font-size:12px;font-weight:700;color:var(--texto);margin-bottom:8px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
          .equipe-card-form{display:flex;flex-direction:column;gap:6px}
          .equipe-card-form input[type=file]{font-size:10.5px;width:100%}
          .equipe-card-form button{width:100%;justify-content:center}
        </style>
        <div class="equipe-aviso">Fotos usadas apenas no site publico (lauroucpcde.com) - padronizadas pelo Marketing. Nao tem relacao com a foto que cada ligante/diretivo anexa no proprio cadastro interno.</div>

        <div class="equipe-bloco">
          <div class="equipe-bloco-header">
            <div class="equipe-bloco-icone" style="background:#7c3aed">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            </div>
            <div class="equipe-bloco-titulo">Diretivos</div>
            <div class="equipe-bloco-contagem"><%=equipeDiretivos.length%> pessoa<%=equipeDiretivos.length!==1?'s':''%></div>
          </div>
          <div class="equipe-grid">
            <%equipeDiretivos.forEach(function(p){%>
            <div class="equipe-card">
              <img src="<%=p.foto_url||'/img/placeholder.png'%>" alt="<%=p.nome%>" onerror="this.onerror=null;this.src='/img/placeholder.png'">
              <div class="equipe-card-corpo">
                <div class="equipe-card-nome" title="<%=p.nome%>"><%=p.nome%></div>
                <form class="equipe-card-form" action="/marketing/equipe/diretivo/<%=p.id%>/foto" method="POST" enctype="multipart/form-data">
                  <input type="file" name="foto" accept="image/*">
                  <button type="submit" class="btn-xs" style="background:#7c3aed;color:white;border-color:transparent">Trocar foto</button>
                </form>
              </div>
            </div>
            <%});%>
          </div>
        </div>

        <div class="equipe-bloco">
          <div class="equipe-bloco-header">
            <div class="equipe-bloco-icone" style="background:#0F6E56">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            </div>
            <div class="equipe-bloco-titulo">Ligantes</div>
            <div class="equipe-bloco-contagem"><%=equipeLigantes.length%> pessoa<%=equipeLigantes.length!==1?'s':''%></div>
          </div>
          <div class="equipe-grid">
            <%equipeLigantes.forEach(function(p){%>
            <div class="equipe-card">
              <img src="<%=p.foto_url||'/img/placeholder.png'%>" alt="<%=p.nome%>" onerror="this.onerror=null;this.src='/img/placeholder.png'">
              <div class="equipe-card-corpo">
                <div class="equipe-card-nome" title="<%=p.nome%>"><%=p.nome%></div>
                <form class="equipe-card-form" action="/marketing/equipe/ligante/<%=p.id%>/foto" method="POST" enctype="multipart/form-data">
                  <input type="file" name="foto" accept="image/*">
                  <button type="submit" class="btn-xs" style="background:#0F6E56;color:white;border-color:transparent">Trocar foto</button>
                </form>
              </div>
            </div>
            <%});%>
          </div>
        </div>

        <div class="equipe-bloco">
          <div class="equipe-bloco-header">
            <div class="equipe-bloco-icone" style="background:#f59e0b">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
            </div>
            <div class="equipe-bloco-titulo">Banners Gerais</div>
            <div class="equipe-bloco-contagem"><%=siteBanners.length%> banner<%=siteBanners.length!==1?'s':''%></div>
          </div>
          <p style="font-size:12px;color:var(--texto2);margin-bottom:14px">Propagandas e avisos do Marketing sem vinculo com um evento especifico. Banner de evento continua sendo gerido direto na aba Eventos (aparece/some sozinho conforme a data do evento).</p>
          <form action="/marketing/banners" method="POST" enctype="multipart/form-data" style="margin-bottom:16px;padding:14px;background:var(--fundo);border-radius:var(--rad)">
            <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:flex-end">
              <div class="campo" style="flex:1;min-width:140px"><label class="label">Titulo (opcional)</label><input type="text" name="titulo" class="input" placeholder="Ex: Inscricoes abertas"></div>
              <div class="campo" style="flex:1;min-width:140px"><label class="label">Link (opcional)</label><input type="text" name="link_url" class="input" placeholder="https://..."></div>
              <div class="campo" style="flex:1;min-width:160px"><label class="label">Imagem</label><input type="file" name="imagem" class="input" accept="image/*" required></div>
              <button type="submit" class="btn btn-primario btn-sm">+ Adicionar banner</button>
            </div>
            <div style="font-size:11px;color:var(--texto2);margin-top:8px">Tamanho recomendado para a imagem: 1920x1080px (16:9), formato JPG ou PNG</div>
          </form>
          <div class="equipe-grid">
            <%siteBanners.forEach(function(b){%>
            <div class="equipe-card">
              <img src="<%=b.imagem_url||'/img/placeholder.png'%>" alt="<%=b.titulo||'Banner'%>" onerror="this.onerror=null;this.src='/img/placeholder.png'">
              <div class="equipe-card-corpo">
                <div class="equipe-card-nome" title="<%=b.titulo||'(sem titulo)'%>"><%=b.titulo||'(sem titulo)'%></div>
                <div style="display:flex;gap:6px">
                  <form action="/marketing/banners/<%=b.id%>/toggle" method="POST" style="flex:1;margin:0">
                    <button type="submit" class="btn-xs" style="width:100%;justify-content:center;background:<%=b.ativo?'#0F6E56':'#94a3b8'%>;color:white;border-color:transparent"><%=b.ativo?'Ativo':'Inativo'%></button>
                  </form>
                  <form action="/marketing/banners/<%=b.id%>/deletar" method="POST" style="margin:0" onsubmit="var f=this; confirmarAcao('Excluir este banner?', function(){ f.submit(); }); return false;">
                    <button type="submit" class="btn-xs verm" style="border-color:#fecaca">Excluir</button>
                  </form>
                </div>
              </div>
            </div>
            <%});%>
          </div>
        </div>

        <div class="equipe-bloco">
          <div class="equipe-bloco-header">
            <div class="equipe-bloco-icone" style="background:#6366f1">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2" ry="2"/></svg>
            </div>
            <div class="equipe-bloco-titulo">Video Institucional</div>
            <div class="equipe-bloco-contagem"><%=siteVideoUrl?'Ativo':'Nenhum video'%></div>
          </div>
          <p style="font-size:12px;color:var(--texto2);margin-bottom:14px">Video de apresentacao exibido no site institucional (lauroucpcde.com). Ao subir um novo video, ele substitui automaticamente o que estiver no ar.</p>
          <%if(siteVideoUrl){%>
          <video src="<%=siteVideoUrl%>" controls style="width:100%;max-width:480px;border-radius:var(--rad);margin-bottom:14px;display:block;background:#000"></video>
          <%}%>
          <form action="/marketing/video" method="POST" enctype="multipart/form-data" style="margin-bottom:12px;padding:14px;background:var(--fundo);border-radius:var(--rad)">
            <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:flex-end">
              <div class="campo" style="flex:1;min-width:200px"><label class="label">Arquivo de video</label><input type="file" name="video" class="input" accept="video/*" required></div>
              <button type="submit" class="btn btn-primario btn-sm"><%=siteVideoUrl?'Substituir video':'+ Adicionar video'%></button>
            </div>
            <div style="font-size:11px;color:var(--texto2);margin-top:8px">Formato MP4 recomendado, 16:9</div>
          </form>
          <%if(siteVideoUrl){%>
          <form action="/marketing/video/remover" method="POST" style="margin:0" onsubmit="var f=this; confirmarAcao('Remover o video do site?', function(){ f.submit(); }); return false;">
            <button type="submit" class="btn-xs verm" style="border-color:#fecaca">Remover video do site</button>
          </form>
          <%}%>
        </div>

        <div class="equipe-bloco">
          <div class="equipe-bloco-header">
            <div class="equipe-bloco-icone" style="background:#0891b2">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 16l4.586-4.586a2 2 0 0 1 2.828 0L16 16"/><path d="M14 14l1.586-1.586a2 2 0 0 1 2.828 0L20 14"/><rect x="2" y="4" width="20" height="16" rx="2"/><circle cx="8" cy="9" r="1.5"/></svg>
            </div>
            <div class="equipe-bloco-titulo">Galeria de Eventos</div>
            <div class="equipe-bloco-contagem"><%=galerias.length%> galeria<%=galerias.length!==1?'s':''%></div>
          </div>
          <p style="font-size:12px;color:var(--texto2);margin-bottom:14px">Fotos de eventos (aulas, corridas, festas) exibidas em galeria no site institucional, para o publico visualizar e salvar. Toda foto enviada aqui recebe automaticamente a marca dagua da liga, se configurada.</p>

          <div style="padding:14px;background:var(--fundo);border-radius:var(--rad);margin-bottom:16px">
            <div class="label" style="margin-bottom:8px">Marca dagua (carimbo)</div>
            <div style="display:flex;align-items:center;gap:14px;flex-wrap:wrap">
              <%if(marcaDaguaUrl){%>
              <img src="<%=marcaDaguaUrl%>" alt="Marca dagua" style="width:80px;height:80px;object-fit:contain;background:repeating-conic-gradient(#e5e7eb 0% 25%,#fff 0% 50%) 50%/16px 16px;border-radius:8px;border:1px solid var(--borda)">
              <%}else{%>
              <div style="width:80px;height:80px;display:flex;align-items:center;justify-content:center;font-size:11px;color:var(--texto2);background:var(--card);border:1px dashed var(--borda);border-radius:8px;text-align:center">Nenhuma marca configurada</div>
              <%}%>
              <form action="/marketing/marca-dagua" method="POST" enctype="multipart/form-data" style="display:flex;gap:8px;align-items:flex-end;flex-wrap:wrap">
                <div class="campo" style="min-width:200px"><label class="label">Imagem PNG (fundo transparente)</label><input type="file" name="marca" class="input" accept="image/png" required></div>
                <button type="submit" class="btn btn-primario btn-sm"><%=marcaDaguaUrl?'Substituir':'+ Definir marca'%></button>
              </form>
              <div style="font-size:11px;color:var(--texto2);width:100%">Tamanho recomendado: 500x500px, PNG com fundo transparente</div>
              <%if(marcaDaguaUrl){%>
              <form action="/marketing/marca-dagua/remover" method="POST" style="margin:0" onsubmit="var f=this; confirmarAcao('Remover a marca dagua?', function(){ f.submit(); }); return false;">
                <button type="submit" class="btn-xs verm" style="border-color:#fecaca">Remover</button>
              </form>
              <%}%>
            </div>
          </div>

          <form action="/marketing/galerias" method="POST" style="margin-bottom:16px;padding:14px;background:var(--fundo);border-radius:var(--rad)">
            <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:flex-end">
              <div class="campo" style="flex:1;min-width:180px"><label class="label">Nome do evento</label><input type="text" name="nome_evento" class="input" placeholder="Ex: Corrida da Liga 2026" required></div>
              <div class="campo" style="min-width:160px"><label class="label">Data (opcional)</label><input type="date" name="data_evento" class="input"></div>
              <button type="submit" class="btn btn-primario btn-sm">+ Criar galeria</button>
            </div>
          </form>

          <div style="display:flex;flex-direction:column;gap:14px">
            <%galerias.forEach(function(g){%>
            <div style="padding:14px;background:var(--fundo);border-radius:var(--rad)">
              <div style="display:flex;justify-content:space-between;align-items:center;gap:8px;flex-wrap:wrap;margin-bottom:10px">
                <div>
                  <div style="font-weight:700;font-size:14px"><%=g.nome_evento%></div>
                  <div style="font-size:11px;color:var(--texto2)"><%=g.data_evento||'Sem data'%> · <%=g.total_fotos%> foto<%=g.total_fotos!==1?'s':''%></div>
                </div>
                <div style="display:flex;gap:6px">
                  <form action="/marketing/galerias/<%=g.id%>/toggle" method="POST" style="margin:0">
                    <button type="submit" class="btn-xs" style="background:<%=g.ativo?'#0F6E56':'#94a3b8'%>;color:white;border-color:transparent"><%=g.ativo?'Ativa':'Inativa'%></button>
                  </form>
                  <form action="/marketing/galerias/<%=g.id%>/deletar" method="POST" style="margin:0" onsubmit="var f=this; confirmarAcao('Excluir esta galeria e todas as fotos?', function(){ f.submit(); }); return false;">
                    <button type="submit" class="btn-xs verm" style="border-color:#fecaca">Excluir galeria</button>
                  </form>
                </div>
              </div>
              <form action="/marketing/galerias/<%=g.id%>/fotos" method="POST" enctype="multipart/form-data" style="margin-bottom:10px">
                <div style="display:flex;gap:8px;align-items:flex-end;flex-wrap:wrap">
                  <div class="campo" style="flex:1;min-width:220px"><label class="label">Adicionar fotos (selecao multipla)</label><input type="file" name="fotos" class="input" accept="image/*" multiple required></div>
                  <button type="submit" class="btn btn-secundario btn-sm">Enviar fotos</button>
                </div>
                <div style="font-size:11px;color:var(--texto2);margin-top:8px">Tamanho recomendado: 1920x1080px (16:9), formato JPG ou PNG</div>
              </form>
              <%if(g.fotos.length){%>
              <div class="equipe-grid">
                <%g.fotos.forEach(function(f){%>
                <div class="equipe-card">
                  <img src="<%=f.url||'/img/placeholder.png'%>" alt="Foto" onerror="this.onerror=null;this.src='/img/placeholder.png'">
                  <div class="equipe-card-corpo">
                    <form action="/marketing/galeria-fotos/<%=f.id%>/deletar" method="POST" style="margin:0" onsubmit="var fo=this; confirmarAcao('Excluir esta foto?', function(){ fo.submit(); }); return false;">
                      <button type="submit" class="btn-xs verm" style="width:100%;justify-content:center;border-color:#fecaca">Excluir</button>
                    </form>
                  </div>
                </div>
                <%});%>
              </div>
              <%}%>
            </div>
            <%});%>
          </div>
        </div>
      </div>
      <div class="mkt-section" id="tab-whatsapp">
        <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:16px">
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Envio em Massa</div>
            <form action="/marketing/whatsapp-massa" method="POST">
              <div style="display:flex;flex-direction:column;gap:12px">
                <div class="campo"><label class="label">Destinatarios</label>
                  <select name="destinatarios" class="input">
                    <option value="todos">Todos (Ligantes + Diretivos)</option>
                    <option value="ligantes">Somente Ligantes</option>
                    <option value="diretivos">Somente Diretivos</option>
                  </select>
                </div>
                <div class="campo"><label class="label">Mensagem</label>
                  <textarea name="mensagem" id="wpp-mensagem" class="input" rows="5" placeholder="Digite a mensagem..." oninput="previewWpp(this.value)"></textarea>
                </div>
                <button type="submit" class="btn btn-primario" onclick="var f=this.form; confirmarAcao('Enviar para todos?', function(){ f.submit(); }); return false;">Enviar Agora</button>
              </div>
            </form>
          </div>
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Preview</div>
            <div class="whatsapp-preview">
              <div class="wpp-bubble">Ola! Como vai?</div>
              <div class="wpp-bubble saida" id="wpp-preview-text">Sua mensagem aparecera aqui...</div>
            </div>
          </div>
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Templates Rapidos</div>
            <button class="template-btn" onclick="usarTemplate(this)">Convite para evento da Liga — prezado ligante, convidamos voce para participar do nosso proximo evento...</button>
            <button class="template-btn" onclick="usarTemplate(this)">Lembrete de mensalidade — informamos que sua mensalidade esta em aberto. Realize o pagamento para continuar ativo...</button>
            <button class="template-btn" onclick="usarTemplate(this)">Aviso de reuniao — comunicamos que havera reuniao da diretoria na proxima semana. Sua presenca e importante...</button>
            <button class="template-btn" onclick="usarTemplate(this)">Boas-vindas ao novo ligante — seja bem-vindo a Liga! Estamos felizes em ter voce conosco...</button>
          </div>
        </div>
      </div>
      <div class="mkt-section" id="tab-integracao">
        <style>
          .ig-feed-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:16px;margin-top:16px;}
          .ig-feed-card{background:#fff;border-radius:10px;overflow:hidden;box-shadow:0 1px 4px rgba(0,0,0,.12);cursor:pointer;transition:transform .2s;display:flex;flex-direction:column;}
          .ig-feed-card:hover{transform:translateY(-2px);box-shadow:0 4px 16px rgba(0,0,0,.15);}
          .ig-feed-img{width:100%;height:180px;object-fit:cover;background:#f1f5f9;display:block;flex-shrink:0;}
          .ig-feed-img-placeholder{width:100%;height:180px;background:#f1f5f9;display:flex;align-items:center;justify-content:center;font-size:32px;flex-shrink:0;}
          .ig-feed-info{padding:10px;flex:1;display:flex;flex-direction:column;justify-content:space-between;}
          .ig-feed-stats{display:flex;gap:10px;font-size:12px;color:#64748b;margin-top:6px;}
          .ig-feed-cover{width:100%;height:180px;flex-shrink:0;background:linear-gradient(135deg,#0d2b1e 0%,#1a3d2b 40%,#2d6a4f 70%,#1a5c3a 100%);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;position:relative;overflow:hidden;}
          .ig-feed-cover::before{content:'';position:absolute;inset:0;background:radial-gradient(circle at 70% 30%,rgba(45,106,79,.6) 0%,transparent 60%);}
          .ig-feed-cover::after{content:'LAURO';position:absolute;bottom:8px;right:10px;font-size:9px;font-weight:800;letter-spacing:2px;color:rgba(255,255,255,.3);}
          .ig-perfil-box{background:linear-gradient(135deg,#833ab4,#fd1d1d,#fcb045);border-radius:12px;padding:20px;color:white;margin-bottom:20px;display:flex;align-items:center;gap:16px;}
          .ig-perfil-avatar{width:60px;height:60px;border-radius:50%;border:3px solid white;object-fit:cover;}
          .ig-perfil-stats{display:flex;gap:20px;margin-top:8px;}
          .ig-perfil-stat{text-align:center;}
          .ig-perfil-stat-num{font-size:18px;font-weight:700;}
          .ig-perfil-stat-label{font-size:11px;opacity:.85;}
          .modal-comentarios{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:9999;align-items:center;justify-content:center;}
          .modal-comentarios.ativo{display:flex;}
          .modal-com-box{background:#fff;border-radius:12px;width:90%;max-width:500px;max-height:80vh;display:flex;flex-direction:column;overflow:hidden;}
          .modal-com-header{background:#1a3d2b;color:white;padding:16px 20px;display:flex;justify-content:space-between;align-items:center;flex-shrink:0;border-radius:12px 12px 0 0;}
          .modal-com-header h3{margin:0;color:white;font-size:15px;font-weight:700;}
          .modal-com-btn-fechar{background:rgba(255,255,255,.15);border:none;border-radius:6px;width:32px;height:32px;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:background .2s;flex-shrink:0;}
          .modal-com-btn-fechar:hover{background:rgba(255,255,255,.3);}
          .modal-com-body{overflow-y:auto;padding:16px 20px;flex:1;}
          .com-item{padding:12px 0;border-bottom:1px solid #f1f5f9;}
          .com-item:last-child{border-bottom:none;}
          .com-header{display:flex;align-items:center;gap:8px;margin-bottom:4px;}
          .com-avatar{width:38px;height:38px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;font-size:13px;font-weight:800;color:#fff;text-shadow:0 1px 2px rgba(0,0,0,.2);}
          .com-avatar img{width:38px;height:38px;border-radius:50%;object-fit:cover;}
          .com-username{font-weight:700;font-size:13px;color:#0f172a;}
          .com-texto{font-size:13px;color:#475569;margin-top:2px;line-height:1.5;}
          .com-data{font-size:11px;color:#94a3b8;margin-top:4px;}
          .com-reply-box{background:#f8fafc;border-radius:8px;padding:10px;margin-left:36px;margin-top:6px;border-left:2px solid #1a3d2b;}
          .com-reply-username{font-weight:700;font-size:12px;color:#1a3d2b;}
          .com-reply-texto{font-size:12px;color:#475569;margin-top:2px;}
        </style>

        <!-- PERFIL INSTAGRAM -->
        <div class="ig-perfil-box" id="ig-perfil-box">
          <div style="width:60px;height:60px;border-radius:50%;background:rgba(255,255,255,.2);display:flex;align-items:center;justify-content:center;font-size:24px">📸</div>
          <div>
            <div style="font-weight:700;font-size:16px">@lauroucp.cde</div>
            <div style="font-size:13px;opacity:.85">Carregando perfil...</div>
            <div class="ig-perfil-stats" id="ig-perfil-stats"></div>
          </div>
        </div>

        <!-- ABAS INSTAGRAM -->
        <div style="display:flex;gap:8px;margin-bottom:16px;flex-wrap:wrap" id="ig-abas">
          <button class="btn btn-sm" id="ig-btn-feed" style="background:#833ab4;color:white;border-color:#833ab4" onclick="igTab('feed')">Feed</button>
          <button class="btn btn-sm" id="ig-btn-config" onclick="igTab('config')">Credenciais</button>
        </div>

        <!-- FEED -->
        <div id="ig-tab-feed">
          <div style="display:flex;justify-content:space-between;align-items:center">
            <h3 style="margin:0">Posts do Instagram</h3>
            <span style="font-size:12px;color:#94a3b8">Todos os posts, incluindo os feitos pelo celular</span>
          </div>
          <div class="ig-feed-grid" id="ig-feed-grid">
            <div style="grid-column:1/-1;text-align:center;padding:40px;color:#94a3b8">Carregando feed...</div>
          </div>
        </div>

        <!-- CONFIG -->
        <div id="ig-tab-config" style="display:none">
          <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:16px">
            <div class="integ-card">
              <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
                <div class="integ-logo" style="background:linear-gradient(45deg,#f09433,#e6683c,#dc2743,#cc2366,#bc1888)"><svg width="20" height="20" viewBox="0 0 24 24" fill="white"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="4" fill="none" stroke="white" stroke-width="1.5"/><circle cx="17.5" cy="6.5" r="1" fill="white"/></svg></div>
                <div><div style="font-weight:700">Instagram</div><div style="font-size:12px;color:var(--texto2)">Meta Graph API</div></div>
                <%if(mktConfig.instagram_token){%><span class="badge-conectado" style="margin-left:auto">Conectado</span><%}else{%><span class="badge-desconectado" style="margin-left:auto">Desconectado</span><%}%>
              </div>
              <form action="/marketing/config/instagram" method="POST">
                <div class="campo"><label class="label">Access Token</label><input name="instagram_token" class="input" type="password" placeholder="Token da API Meta" value="<%=mktConfig.instagram_token||''%>"></div>
                <div class="campo"><label class="label">Instagram Business ID</label><input name="instagram_id" class="input" placeholder="ID da conta business" value="<%=mktConfig.instagram_id||''%>"></div>
                <button type="submit" class="btn btn-primario" style="width:100%;margin-top:8px">Salvar</button>
              </form>
            </div>
            <div class="integ-card">
              <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
                <div class="integ-logo" style="background:#1877f2"><svg width="20" height="20" viewBox="0 0 24 24" fill="white"><path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"/></svg></div>
                <div><div style="font-weight:700">Facebook</div><div style="font-size:12px;color:var(--texto2)">Meta Graph API</div></div>
                <%if(mktConfig.facebook_token){%><span class="badge-conectado" style="margin-left:auto">Conectado</span><%}else{%><span class="badge-desconectado" style="margin-left:auto">Desconectado</span><%}%>
              </div>
              <form action="/marketing/config/facebook" method="POST">
                <div class="campo"><label class="label">Page Access Token</label><input name="facebook_token" class="input" type="password" placeholder="Token da pagina" value="<%=mktConfig.facebook_token||''%>"></div>
                <div class="campo"><label class="label">Facebook Page ID</label><input name="facebook_id" class="input" placeholder="ID da pagina" value="<%=mktConfig.facebook_id||''%>"></div>
                <button type="submit" class="btn btn-primario" style="width:100%;margin-top:8px">Salvar</button>
              </form>
            </div>
            <div class="integ-card">
              <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
                <div class="integ-logo" style="background:#25d366"><svg width="20" height="20" viewBox="0 0 24 24" fill="white"><path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"/></svg></div>
                <div><div style="font-weight:700">WhatsApp</div><div style="font-size:12px;color:var(--texto2)">W-API</div></div>
                <span class="badge-conectado" style="margin-left:auto">Conectado</span>
              </div>
              <p style="font-size:13px;color:var(--texto2)">Integracao W-API ja configurada. Pronta para envio em massa.</p>
            </div>
            <div class="integ-card">
              <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
                <div class="integ-logo" style="background:#00c4cc"><svg width="20" height="20" viewBox="0 0 24 24" fill="white"><path d="M12 2L2 12l10 10 10-10L12 2z"/></svg></div>
                <div><div style="font-weight:700">Canva</div><div style="font-size:12px;color:var(--texto2)">Connect API</div></div>
                <%if(typeof canvaConectado!=='undefined' && canvaConectado){%><span class="badge-conectado" style="margin-left:auto">Conectado</span><%}else{%><span class="badge-desconectado" style="margin-left:auto">Desconectado</span><%}%>
              </div>
              <p style="font-size:13px;color:var(--texto2);margin-bottom:12px">Conecte a conta Canva da Liga para criar e reaproveitar designs direto no post.</p>
              <%if(typeof canvaConectado!=='undefined' && canvaConectado){%>
              <form action="/marketing/canva/desconectar" method="POST">
                <button type="submit" class="btn" style="width:100%;color:#ef4444" onclick="var f=this.form; confirmarAcao('Desconectar a conta Canva?', function(){ f.submit(); }); return false;">Desconectar</button>
              </form>
              <%}else{%>
              <a href="/marketing/canva/conectar" class="btn btn-primario" style="width:100%;text-align:center;text-decoration:none;display:block">Conectar Canva</a>
              <%}%>
            </div>
          </div>
        </div>

        <!-- MODAL COMENTARIOS -->
        <div class="modal-comentarios" id="modal-comentarios" onclick="if(event.target===this)fecharComentarios()">
          <div class="modal-com-box">
            <div class="modal-com-header">
              <h3>Comentarios</h3>
              <button class="modal-com-btn-fechar" onclick="fecharComentarios()" title="Fechar">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
              </button>
            </div>
            <div class="modal-com-body">
              <div id="comentarios-lista"><div style="text-align:center;padding:20px;color:#94a3b8">Carregando...</div></div>
            </div>
          </div>
        </div>
      </div>
      <div class="mkt-section" id="tab-aprovacao">
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px">
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Posts Aguardando Aprovacao</div>
            <%const pendAprov=posts.filter(p=>p.status==='rascunho');%>
            <%if(pendAprov.length===0){%>
            <div style="text-align:center;padding:32px;color:var(--texto2)">Nenhum pendente</div>
            <%}%>
            <%pendAprov.forEach(p=>{%>
            <div class="post-card" style="padding:12px;margin-bottom:8px">
              <div style="font-weight:600;font-size:13px;margin-bottom:4px"><%=p.titulo||'(Sem titulo)'%></div>
              <div style="font-size:11px;color:var(--texto2);margin-bottom:8px"><%=(p.conteudo||'').substring(0,80)%>...</div>
              <div style="display:flex;gap:6px">
                <form method="POST" action="/marketing/<%=p.id%>/publicar" style="display:inline;margin:0">
                  <button type="submit" class="btn-xs verde">Aprovar</button></form>
                <form method="POST" action="/marketing/<%=p.id%>/deletar" style="display:inline;margin:0" onsubmit="var f=this; confirmarAcao('Reprovar?', function(){ f.submit(); }); return false;">
                  <button type="submit" class="btn-xs verm">Reprovar</button></form>
              </div>
            </div>
            <%});%>
          </div>
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Melhores Horarios</div>
            <p style="font-size:12px;color:var(--texto2);margin-bottom:10px">Setor saude/educacao no Brasil</p>
            <div class="horario-grid">
              <%[['Dom',0],['Seg',1],['Ter',1],['Qua',1],['Qui',1],['Sex',1],['Sab',0]].forEach(([d,util])=>{%>
              <div class="horario-dia"><div class="horario-dia-nome"><%=d%></div>
                <div class="horario-slot <%=util?'alto':'baixo'%>">8h</div>
                <div class="horario-slot <%=util?'alto':'medio'%>">12h</div>
                <div class="horario-slot <%=util?'alto':'medio'%>">18h</div>
                <div class="horario-slot medio">21h</div>
              </div><%});%>
            </div>
            <div style="display:flex;gap:8px;margin-top:10px;font-size:11px">
              <span style="background:#dcfce7;color:#166534;padding:2px 8px;border-radius:var(--rad);font-weight:600">Alto</span>
              <span style="background:#fef3c7;color:#92400e;padding:2px 8px;border-radius:var(--rad);font-weight:600">Medio</span>
              <span style="background:#f3f4f6;color:#9ca3af;padding:2px 8px;border-radius:var(--rad);font-weight:600">Baixo</span>
            </div>
          </div>
        </div>
      </div>
      <div class="mkt-section" id="tab-historico">
        <div class="card">
          <div class="card-titulo" style="margin-bottom:14px">Historico de Publicacoes</div>
          <%const pubList=posts.filter(p=>p.status==='publicado');%>
          <%if(pubList.length===0){%>
          <div style="text-align:center;padding:40px;color:var(--texto2)">Nenhuma publicacao ainda</div>
          <%}%>
          <%pubList.forEach(p=>{%>
          <div class="log-item">
            <div class="log-dot" style="background:#10b981"></div>
            <div style="flex:1">
              <div style="font-weight:600;font-size:13px"><%=p.titulo%></div>
              <div style="font-size:11px;color:var(--texto2)"><%=new Date(p.criado_em).toLocaleString('pt-BR')%></div>
            </div>
            <div style="display:flex;gap:4px"><%=(p.redes||[]).map(r=>'<span class="rede-badge rede-'+r+'">'+r+'</span>').join(' ')%></div>
          </div><%});%>
        </div>
      </div>
      <div class="mkt-section" id="tab-analytics">
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px">
          <div class="card"><div class="card-titulo" style="margin-bottom:14px">Por Rede</div>
            <div style="display:flex;flex-direction:column;gap:10px">
              <div style="display:flex;justify-content:space-between;padding:10px;background:#fce7f3;border-radius:var(--rad)">
                <span style="font-weight:600;color:#9d174d">Instagram</span>
                <span style="font-weight:700;font-size:18px;color:#9d174d"><%=posts.filter(p=>(p.redes||[]).includes('instagram')).length%></span>
              </div>
              <div style="display:flex;justify-content:space-between;padding:10px;background:#dbeafe;border-radius:var(--rad)">
                <span style="font-weight:600;color:#1e40af">Facebook</span>
                <span style="font-weight:700;font-size:18px;color:#1e40af"><%=posts.filter(p=>(p.redes||[]).includes('facebook')).length%></span>
              </div>
              <div style="display:flex;justify-content:space-between;padding:10px;background:#dcfce7;border-radius:var(--rad)">
                <span style="font-weight:600;color:#166534">WhatsApp</span>
                <span style="font-weight:700;font-size:18px;color:#166534"><%=posts.filter(p=>(p.redes||[]).includes('whatsapp')).length%></span>
              </div>
            </div>
          </div>
          <div class="card"><div class="card-titulo" style="margin-bottom:14px">Resumo do Mes</div>
            <%const mesAtual=new Date().getMonth();const anoAtual=new Date().getFullYear();
            const postsMes=posts.filter(p=>{const d=new Date(p.criado_em);return d.getMonth()===mesAtual&&d.getFullYear()===anoAtual;});%>
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px">
              <div style="background:var(--fundo);border-radius:var(--rad);padding:12px;text-align:center"><div style="font-size:22px;font-weight:700;color:var(--cor-primaria)"><%=postsMes.length%></div><div style="font-size:11px;color:var(--texto2)">Posts este mes</div></div>
              <div style="background:var(--fundo);border-radius:var(--rad);padding:12px;text-align:center"><div style="font-size:22px;font-weight:700;color:#10b981"><%=postsMes.filter(p=>p.status==='publicado').length%></div><div style="font-size:11px;color:var(--texto2)">Publicados</div></div>
              <div style="background:var(--fundo);border-radius:var(--rad);padding:12px;text-align:center"><div style="font-size:22px;font-weight:700;color:#f59e0b"><%=postsMes.filter(p=>p.status==='agendado').length%></div><div style="font-size:11px;color:var(--texto2)">Agendados</div></div>
              <div style="background:var(--fundo);border-radius:var(--rad);padding:12px;text-align:center"><div style="font-size:22px;font-weight:700;color:#0891b2"><%=midias.length%></div><div style="font-size:11px;color:var(--texto2)">Midias</div></div>
            </div>
          </div>
        </div>
        <div class="card">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">
            <div class="card-titulo">Relatorio Mensal</div>
            <button class="btn btn-sm btn-primario" onclick="gerarRelatorio()">Baixar CSV</button>
          </div>
          <p style="font-size:13px;color:var(--texto2)">Exporta todos os posts com status, redes e datas.</p>
        </div>
      </div>
    </div>
      <div class="mkt-section" id="tab-campanha">
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;max-width:100%">
          <div class="card">
            <div class="card-titulo" style="margin-bottom:4px">Campanha Multicanal</div>
            <p style="font-size:12px;color:var(--texto2);margin-bottom:14px">Dispare para todas as redes com um unico clique</p>
            <form action="/marketing/posts" method="POST" enctype="multipart/form-data">
              <div style="display:flex;flex-direction:column;gap:12px">
                <div class="campo"><label class="label">Titulo *</label>
                  <input name="titulo" class="input" required placeholder="Ex: Semana Urologica 2026">
                </div>
                <div class="campo"><label class="label">Legenda *</label>
                  <textarea name="conteudo" class="input" rows="4" placeholder="Mensagem para todas as redes..."></textarea>
                </div>
                <div class="campo"><label class="label">Imagem</label>
                  <input type="file" name="imagem" class="input" accept="image/*">
                </div>
                <div class="campo"><label class="label">Canais</label>
                  <div style="display:flex;gap:14px;flex-wrap:wrap">
                    <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:13px">
                      <input type="checkbox" name="redes" value="instagram" checked> Instagram
                    </label>
                    <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:13px">
                      <input type="checkbox" name="redes" value="facebook" checked> Facebook
                    </label>
                    <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:13px">
                      <input type="checkbox" name="redes" value="whatsapp" checked> WhatsApp
                    </label>
                  </div>
                </div>
                <button type="submit" name="acao" value="agendar" class="btn btn-primario"
                  onclick="var f=this.form; confirmarAcao('Disparar campanha para todas as redes?', function(){ f.submit(); }); return false;">Disparar Campanha</button>
              </div>
            </form>
          </div>
          <div class="card">
            <div class="card-titulo" style="margin-bottom:14px">Como funciona</div>
            <div style="display:flex;flex-direction:column;gap:14px">
              <div style="display:flex;gap:12px;align-items:flex-start">
                <div style="width:28px;height:28px;border-radius:50%;background:var(--cor-primaria);color:white;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;flex-shrink:0">1</div>
                <div><div style="font-weight:600;font-size:13px">Crie o conteudo</div>
                <div style="font-size:12px;color:var(--texto2)">Escreva a legenda e adicione imagem</div></div>
              </div>
              <div style="display:flex;gap:12px;align-items:flex-start">
                <div style="width:28px;height:28px;border-radius:50%;background:var(--cor-primaria);color:white;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;flex-shrink:0">2</div>
                <div><div style="font-weight:600;font-size:13px">Selecione os canais</div>
                <div style="font-size:12px;color:var(--texto2)">Instagram, Facebook e/ou WhatsApp</div></div>
              </div>
              <div style="display:flex;gap:12px;align-items:flex-start">
                <div style="width:28px;height:28px;border-radius:50%;background:#10b981;color:white;display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;flex-shrink:0">3</div>
                <div><div style="font-weight:600;font-size:13px">Disparo automatico</div>
                <div style="font-size:12px;color:var(--texto2)">Sistema publica em todos os canais</div></div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="mkt-section" id="tab-templates">
        <div style="margin-bottom:16px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px">
          <div style="font-size:14px;font-weight:700">Templates Prontos</div>
          <span style="font-size:12px;color:var(--texto2)">Clique para usar como base no novo post</span>
        </div>
        <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:12px">
          <div class="post-card" style="cursor:pointer" onclick="usarTemplate2('Semana Urologica','Prezados ligantes, convidamos voce para a nossa Semana Urologica 2026! Um evento repleto de palestras, workshops e discussoes de casos clinicos. Sua presenca e fundamental! Data: a confirmar. #urologia #medicinaacademica #ligaurologia')">
            <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
              <div style="width:36px;height:36px;border-radius:var(--rad);background:#eff6ff;display:flex;align-items:center;justify-content:center"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#1d4ed8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg></div>
              <div><div style="font-weight:700;font-size:13px">Semana Urologica</div><div style="font-size:11px;color:var(--texto2)">Evento cientifico</div></div>
            </div>
            <p style="font-size:12px;color:var(--texto2);line-height:1.5">Convite para evento academico da liga com palestras e workshops.</p>
            <div style="margin-top:8px"><span class="rede-badge rede-instagram">instagram</span> <span class="rede-badge rede-facebook">facebook</span></div>
          </div>
          <div class="post-card" style="cursor:pointer" onclick="usarTemplate2('Boas-vindas Ligante','Sejam bem-vindos aos novos integrantes da Liga Academica de Urologia! Estamos muito felizes em ter voces conosco. Juntos vamos crescer, aprender e fazer historia na urologia academica. #ligaurologia #bemvindo #medicinaacademica')">
            <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
              <div style="width:36px;height:36px;border-radius:var(--rad);background:#f0fdf4;display:flex;align-items:center;justify-content:center"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#16a34a" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg></div>
              <div><div style="font-weight:700;font-size:13px">Boas-vindas</div><div style="font-size:11px;color:var(--texto2)">Novos membros</div></div>
            </div>
            <p style="font-size:12px;color:var(--texto2);line-height:1.5">Mensagem de boas-vindas para novos integrantes da liga.</p>
            <div style="margin-top:8px"><span class="rede-badge rede-whatsapp">whatsapp</span> <span class="rede-badge rede-instagram">instagram</span></div>
          </div>
          <div class="post-card" style="cursor:pointer" onclick="usarTemplate2('Congresso de Urologia','A Liga Academica de Urologia estara presente no Congresso Brasileiro de Urologia! Venha nos encontrar e trocar experiencias. Um orgulho representar nossa liga neste grande evento. #CBU #urologia #congresso')">
            <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
              <div style="width:36px;height:36px;border-radius:var(--rad);background:#fdf4ff;display:flex;align-items:center;justify-content:center"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#7c3aed" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg></div>
              <div><div style="font-weight:700;font-size:13px">Congresso</div><div style="font-size:11px;color:var(--texto2)">Representacao em evento</div></div>
            </div>
            <p style="font-size:12px;color:var(--texto2);line-height:1.5">Post sobre participacao em congresso nacional de urologia.</p>
            <div style="margin-top:8px"><span class="rede-badge rede-instagram">instagram</span> <span class="rede-badge rede-facebook">facebook</span></div>
          </div>
          <div class="post-card" style="cursor:pointer" onclick="usarTemplate2('Lembrete Mensalidade','Prezado ligante, lembramos que sua mensalidade esta em aberto. Para continuar ativo e aproveitando todos os beneficios da liga, realize o pagamento ate o fim do mes. Qualquer duvida, entre em contato conosco.')">
            <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
              <div style="width:36px;height:36px;border-radius:var(--rad);background:#fffbeb;display:flex;align-items:center;justify-content:center"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#d97706" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg></div>
              <div><div style="font-weight:700;font-size:13px">Lembrete</div><div style="font-size:11px;color:var(--texto2)">Cobranca mensalidade</div></div>
            </div>
            <p style="font-size:12px;color:var(--texto2);line-height:1.5">Mensagem de lembrete de mensalidade via WhatsApp.</p>
            <div style="margin-top:8px"><span class="rede-badge rede-whatsapp">whatsapp</span></div>
          </div>
          <div class="post-card" style="cursor:pointer" onclick="usarTemplate2('Divulgacao Pesquisa','Nossa liga esta desenvolvendo uma pesquisa cientifica na area de urologia! Seu apoio e fundamental para o avanco da medicina academica. Acompanhe nossas redes para novidades. #pesquisa #urologia #ciencia')">
            <div style="display:flex;align-items:center;gap:10px;margin-bottom:8px">
              <div style="width:36px;height:36px;border-radius:var(--rad);background:#fff1f2;display:flex;align-items:center;justify-content:center"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#e11d48" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg></div>
              <div><div style="font-weight:700;font-size:13px">Pesquisa</div><div style="font-size:11px;color:var(--texto2)">Divulgacao cientifica</div></div>
            </div>
            <p style="font-size:12px;color:var(--texto2);line-height:1.5">Post de divulgacao de pesquisa cientifica da liga.</p>
            <div style="margin-top:8px"><span class="rede-badge rede-instagram">instagram</span> <span class="rede-badge rede-facebook">facebook</span></div>
          </div>
        </div>
      </div>
      </div>
    </div>
  </main>
</div>
<div class="modal-overlay" id="modal-novo-post" style="display:none">
  <div class="modal" style="max-width:600px">
    <div class="modal-header"><span class="modal-titulo">Novo Post</span><button class="icon-btn" onclick="fecharModal('modal-novo-post')">&#x2715;</button></div>
    <form action="/marketing/posts" method="POST" enctype="multipart/form-data">
      <div style="display:flex;flex-direction:column;gap:14px">
        <div class="campo"><label class="label">Titulo</label><input name="titulo" class="input" placeholder="Titulo do post" required></div>
        <div class="campo">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px">
            <label class="label" style="margin:0">Conteudo / Legenda</label>
            <button type="button" class="btn-xs roxo" onclick="gerarComIA()">Gerar com IA</button>
          </div>
          <div class="ia-loading" id="ia-loading"><div class="dots"><span>.</span><span>.</span><span>.</span></div> IA gerando legenda...</div>
          <textarea name="conteudo" id="post-conteudo" class="input" rows="4" placeholder="Escreva a legenda do post..."></textarea>
        </div>
        <div class="campo"><label class="label">Imagem</label><input type="file" name="imagem" class="input" accept="image/*"></div>
        <div class="campo"><label class="label">Redes Sociais</label>
          <div style="display:flex;gap:16px;flex-wrap:wrap">
            <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:13px"><input type="checkbox" name="redes" value="instagram"> Instagram</label>
            <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:13px"><input type="checkbox" name="redes" value="facebook"> Facebook</label>
            <label style="display:flex;align-items:center;gap:6px;cursor:pointer;font-size:13px"><input type="checkbox" name="redes" value="whatsapp"> WhatsApp</label>
          </div>
        </div>
        <div class="campo"><label class="label">Agendar para (opcional)</label><input type="datetime-local" name="agendado_para" class="input" onchange="calcularMelhorHorario(this.value)"></div>
        <div class="campo">
          <label class="label">Hashtags (clique para adicionar)</label>
          <div class="ia-loading" id="hashtag-loading"><div class="dots"><span>.</span><span>.</span><span>.</span></div> Buscando hashtags...</div>
          <div class="hashtag-grid" id="hashtag-grid"></div>
          <input type="hidden" name="hashtags" id="hashtags-selecionadas">
          <button type="button" class="btn-xs roxo" style="margin-top:8px" onclick="gerarHashtags()">Sugerir hashtags com IA</button>
        </div>
        <div class="score-wrap" id="score-wrap">
          <div class="score-label"><span style="font-weight:700">Score do Post</span><span id="score-valor" style="color:var(--cor-primaria)">0/100</span></div>
          <div class="score-bar-wrap"><div class="score-bar-fill" id="score-bar" style="width:0%;background:#ef4444"></div></div>
          <div id="score-dicas" style="font-size:11px;color:var(--texto2);margin-top:6px"></div>
        </div>
      </div>
      <div class="campo">
        <label class="label">Preview</label>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
          <div>
            <div style="font-size:11px;font-weight:700;color:#e1306c;margin-bottom:6px">Instagram</div>
            <div id="prev-ig" style="background:#fafafa;border:1px solid #dbdbdb;border-radius:var(--rad);overflow:hidden;max-width:220px">
              <div style="display:flex;align-items:center;gap:8px;padding:8px 10px;border-bottom:1px solid #efefef">
                <div style="width:28px;height:28px;border-radius:50%;background:linear-gradient(45deg,#f09433,#e6683c,#dc2743,#cc2366)"></div>
                <span style="font-size:12px;font-weight:700">liga_urologia</span>
              </div>
              <div id="prev-ig-img" style="width:100%;height:120px;background:#f0f0f0;display:flex;align-items:center;justify-content:center;color:#aaa;font-size:12px">Sem imagem</div>
              <div style="padding:8px 10px">
                <div style="font-size:11px;line-height:1.4" id="prev-ig-txt">Legenda aparece aqui...</div>
              </div>
            </div>
          </div>
          <div>
            <div style="font-size:11px;font-weight:700;color:#25d366;margin-bottom:6px">WhatsApp</div>
            <div style="background:#e5ddd5;border-radius:var(--rad);padding:10px;min-height:120px">
              <div class="wpp-bubble saida" id="prev-wpp-txt" style="font-size:11px">Mensagem aparece aqui...</div>
            </div>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn" onclick="fecharModal('modal-novo-post')">Cancelar</button>
        <button type="submit" name="acao" value="rascunho" class="btn">Rascunho</button>
        <button type="submit" name="acao" value="agendar" class="btn btn-primario">Agendar</button>
      </div>
    </form>
  </div>
</div>
<div class="modal-overlay" id="modal-wpp-massa" style="display:none">
  <div class="modal">
    <div class="modal-header"><span class="modal-titulo">WhatsApp em Massa</span><button class="icon-btn" onclick="fecharModal('modal-wpp-massa')">&#x2715;</button></div>
    <form action="/marketing/whatsapp-massa" method="POST">
      <div style="display:flex;flex-direction:column;gap:14px">
        <div class="campo"><label class="label">Destinatarios</label>
          <select name="destinatarios" class="input">
            <option value="todos">Todos (Ligantes + Diretivos)</option>
            <option value="ligantes">Somente Ligantes</option>
            <option value="diretivos">Somente Diretivos</option>
          </select>
        </div>
        <div class="campo"><label class="label">Mensagem</label>
          <textarea name="mensagem" class="input" rows="5" placeholder="Digite a mensagem..."></textarea>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn" onclick="fecharModal('modal-wpp-massa')">Cancelar</button>
        <button type="submit" class="btn btn-primario" onclick="var f=this.form; confirmarAcao('Enviar para todos?', function(){ f.submit(); }); return false;">Enviar</button>
      </div>
    </form>
  </div>
</div>
<script src="/js/app.js"></script>
<script>
const postsData=<%- JSON.stringify(posts) %>;
let calMes=new Date().getMonth(),calAno=new Date().getFullYear();
// showTab definida no head
function mudarMes(d){calMes+=d;if(calMes<0){calMes=11;calAno--;}if(calMes>11){calMes=0;calAno++;}renderCal();}
const hashtagsSel=new Set();
function calcScore(){
  const titulo=document.querySelector('[name="titulo"]')?.value||'';
  const conteudo=document.getElementById('post-conteudo')?.value||'';
  const imagem=document.querySelector('[name="imagem"]')?.files?.length>0;
  const redes=document.querySelectorAll('[name="redes"]:checked').length;
  const agendado=document.querySelector('[name="agendado_para"]')?.value;
  let score=0;const itens=[];
  if(titulo.length>5){score+=20;itens.push({ok:true,txt:'Titulo preenchido'});}else{itens.push({ok:false,txt:'Adicione um titulo'});}
  if(conteudo.length>30){score+=20;itens.push({ok:true,txt:'Legenda boa'});}else{itens.push({ok:false,txt:'Escreva uma legenda'});}
  if(conteudo.length>=100&&conteudo.length<=2200){score+=15;itens.push({ok:true,txt:'Tamanho ideal'});}else if(conteudo.length>0){itens.push({ok:false,txt:'Tamanho nao ideal'});}
  if(imagem){score+=20;itens.push({ok:true,txt:'Tem imagem'});}else{itens.push({ok:false,txt:'Adicione uma imagem'});}
  if(redes>0){score+=15;itens.push({ok:true,txt:redes+' rede(s)'});}else{itens.push({ok:false,txt:'Selecione redes'});}
  if(hashtagsSel.size>0){score+=10;itens.push({ok:true,txt:hashtagsSel.size+' hashtag(s)'});}else{itens.push({ok:false,txt:'Adicione hashtags'});}
  const cor=score>=70?'#10b981':score>=40?'#f59e0b':'#ef4444';
  document.getElementById('score-num').textContent=score;
  document.getElementById('score-num').style.color=cor;
  document.getElementById('score-fill').style.width=score+'%';
  document.getElementById('score-fill').style.background=cor;
  document.getElementById('score-itens').innerHTML=itens.map(i=>'<span class="score-item '+(i.ok?'ok':'no')+'">'+( i.ok?'<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:inline;vertical-align:middle"><polyline points="20 6 9 17 4 12"/></svg> ':' ')+i.txt+'</span>').join('');
}
document.addEventListener('input',e=>{if(e.target.closest('#modal-novo-post'))calcScore();});
document.addEventListener('change',e=>{if(e.target.closest('#modal-novo-post'))calcScore();});
async function gerarHashtags(){
  const titulo=document.querySelector('[name="titulo"]')?.value||'';
  const grid=document.getElementById('hashtag-grid');
  grid.innerHTML='<span style="font-size:12px;color:var(--texto2)">Gerando...</span>';
  try{
    const r=await fetch("/marketing/gerar-hashtags",{
      method:"POST",headers:{"Content-Type":"application/json"},
      body:JSON.stringify({titulo})
    });
    const data=await r.json();
    if(!data.ok){grid.innerHTML='<span style="color:#dc2626;font-size:12px">'+(data.erro||'Erro ao gerar')+'</span>';return;}
    grid.innerHTML=data.tags.map(t=>'<span class="hashtag-chip" onclick="toggleHashtag(this,\''+t+'\')">'+t+'</span>').join('');
  }catch(e){grid.innerHTML='<span style="color:#dc2626;font-size:12px">Erro ao gerar</span>';}
}
function toggleHashtag(el,tag){
  el.classList.toggle('selecionado');
  if(el.classList.contains('selecionado'))hashtagsSel.add(tag);else hashtagsSel.delete(tag);
  document.getElementById('hashtags-selecionadas').value=[...hashtagsSel].join(' ');
  calcScore();
}
function usarTemplate2(titulo,conteudo){
  abrirModal('modal-novo-post');
  setTimeout(()=>{
    const t=document.querySelector('#modal-novo-post [name="titulo"]');
    const c=document.getElementById('post-conteudo');
    if(t)t.value=titulo;
    if(c){c.value=conteudo;atualizarPreview();calcScore();}
  },100);
}
function atualizarPreview(){
  const txt=document.getElementById('post-conteudo')?.value||'';
  const ig=document.getElementById('prev-ig-txt');
  const wpp=document.getElementById('prev-wpp-txt');
  if(ig)ig.textContent=txt.substring(0,150)+(txt.length>150?'...':'');
  if(wpp)wpp.textContent=txt.substring(0,200)+(txt.length>200?'...':'');
}
document.addEventListener('input',e=>{
  if(e.target.id==='post-conteudo')atualizarPreview();
});
function gerarRelatorio(){
  const linhas=[['Titulo','Status','Redes','Data']];
  postsData.forEach(p=>linhas.push([
    p.titulo||'(Sem titulo)',
    p.status,
    (p.redes||[]).join('/'),
    new Date(p.criado_em).toLocaleDateString('pt-BR')
  ]));
  const csv=linhas.map(l=>l.map(v=>'"'+(v||'').replace(/"/g,'""')+'"').join(',')).join('\n');
  const blob=new Blob(['\uFEFF'+csv],{type:'text/csv;charset=utf-8'});
  const a=document.createElement('a');
  a.href=URL.createObjectURL(blob);
  a.download='relatorio-marketing.csv';
  a.click();
}
async function gerarComIA(){
  const titulo=document.querySelector('[name="titulo"]').value;
  const loading=document.getElementById('ia-loading');
  loading.classList.add('ativo');
  try{
    const r=await fetch("/marketing/gerar-legenda",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({titulo})});
    const data=await r.json();
    if(!data.ok){alert('Erro ao gerar com IA: '+(data.erro||'tente novamente')); loading.classList.remove('ativo'); return;}
    document.getElementById('post-conteudo').value=data.texto;
    atualizarPreview();
  }catch(e){alert('Erro ao gerar com IA. Tente novamente.');}
  loading.classList.remove('ativo');
}

// ─── INSTAGRAM FEED & COMENTARIOS ────────────────────────────────────────────
let igMediaAtual = null;

function igTab(tab) {
  document.getElementById('ig-tab-feed').style.display = tab === 'feed' ? 'block' : 'none';
  document.getElementById('ig-tab-config').style.display = tab === 'config' ? 'block' : 'none';
  const bf = document.getElementById('ig-btn-feed');
  const bc = document.getElementById('ig-btn-config');
  if(bf && bc) {
    bf.style.background = tab==='feed' ? '#833ab4' : '';
    bf.style.color = tab==='feed' ? 'white' : '';
    bc.style.background = tab==='config' ? '#833ab4' : '';
    bc.style.color = tab==='config' ? 'white' : '';
  }
}

function carregarIgPerfil() {
  fetch('/api/instagram/perfil').then(r=>r.json()).then(d=>{
    if(!d.ok) return;
    const p = d.perfil;
    const box = document.getElementById('ig-perfil-box');
    if(p.profile_picture_url) {
      box.querySelector('div').innerHTML = '<img src="'+p.profile_picture_url+'" class="ig-perfil-avatar">';
    }
    box.querySelector('div:nth-child(2) div:nth-child(2)').textContent = p.biography || '@lauroucp.cde';
    document.getElementById('ig-perfil-stats').innerHTML =
      '<div class="ig-perfil-stat"><div class="ig-perfil-stat-num">'+(p.followers_count||0)+'</div><div class="ig-perfil-stat-label">Seguidores</div></div>'+
      '<div class="ig-perfil-stat"><div class="ig-perfil-stat-num">'+(p.follows_count||0)+'</div><div class="ig-perfil-stat-label">Seguindo</div></div>'+
      '<div class="ig-perfil-stat"><div class="ig-perfil-stat-num">'+(p.media_count||0)+'</div><div class="ig-perfil-stat-label">Posts</div></div>';
  }).catch(()=>{});
}

function carregarIgFeed() {
  fetch('/api/instagram/feed').then(r=>r.json()).then(d=>{
    const grid = document.getElementById('ig-feed-grid');
    if(!d.ok || !d.feed.length) { grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:40px;color:#94a3b8">Nenhum post encontrado.</div>'; return; }
    grid.innerHTML = d.feed.map(p=>{
        const isVideo = p.media_type==='VIDEO'||p.media_type==='REELS';
        const isCarousel = p.media_type==='CAROUSEL_ALBUM';
        const imgSrc = isVideo ? (p.thumbnail_url||null) : p.media_url;
        let mediaHtml;
        if(imgSrc) {
          mediaHtml = '<div style="position:relative;flex-shrink:0">'
            +'<img src="'+imgSrc+'" class="ig-feed-img" onerror="this.parentNode.innerHTML=&quot;<div class=ig-feed-cover><svg width=36 height=36 viewBox=0 0 24 24 fill=none stroke=rgba(255,255,255,0.5) stroke-width=1.5><circle cx=12 cy=12 r=10/><path d=M9 9l6 3-6 3V9z/></svg></div>&quot;">'
            +(isVideo ? '<div style="position:absolute;top:8px;right:8px;background:rgba(0,0,0,.6);border-radius:50%;width:30px;height:30px;display:flex;align-items:center;justify-content:center"><svg width=12 height=12 viewBox=\'0 0 24 24\' fill=\'white\'><polygon points=\'5 3 19 12 5 21 5 3\'/></svg></div>' : '')
            +(isCarousel ? '<div style="position:absolute;top:8px;right:8px;background:rgba(0,0,0,.6);border-radius:4px;padding:2px 6px;font-size:10px;color:white;font-weight:700">+</div>' : '')
            +'</div>';
        } else {
          mediaHtml = '<div class="ig-feed-cover">'
            +'<svg width=36 height=36 viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,0.5)" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M9 9l6 3-6 3V9z"/></svg>'
            +'<span style="font-size:9px;color:rgba(255,255,255,0.4);font-weight:700;letter-spacing:1px;margin-top:2px">VIDEO</span>'
            +'</div>';
        }
        return `<div class="ig-feed-card" onclick="abrirComentarios('${p.id}')">
        ${mediaHtml}
        <div class="ig-feed-info">
          <div style="font-size:11px;color:#374151;line-height:1.4;overflow:hidden;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical">${p.caption||'(sem legenda)'}</div>
          <div>
            <div class="ig-feed-stats">
              <span>❤️ ${p.like_count||0}</span>
              <span>💬 ${p.comments_count||0}</span>
            </div>
            <div style="font-size:10px;color:#94a3b8;margin-top:4px">${new Date(p.timestamp).toLocaleDateString('pt-BR')}</div>
          </div>
        </div>
      </div>`;
      }).join('');
  }).catch(()=>{ document.getElementById('ig-feed-grid').innerHTML='<div style="grid-column:1/-1;text-align:center;padding:40px;color:#94a3b8">Erro ao carregar feed.</div>'; });
}

function abrirComentarios(mediaId) {
  igMediaAtual = mediaId;
  document.getElementById('modal-comentarios').classList.add('ativo');
  document.getElementById('comentarios-lista').innerHTML = '<div style="text-align:center;padding:20px;color:#94a3b8">Carregando...</div>';
  fetch('/api/instagram/comentarios/'+mediaId).then(r=>r.json()).then(d=>{
    if(!d.ok || !d.comentarios.length) {
      document.getElementById('comentarios-lista').innerHTML='<div style="text-align:center;padding:20px;color:#94a3b8">Nenhum comentario encontrado.</div>';
      return;
    }
    document.getElementById('comentarios-lista').innerHTML = (()=>{
    const avatarColors = ['#e11d48','#db2777','#9333ea','#7c3aed','#4f46e5','#2563eb','#0284c7','#0891b2','#059669','#16a34a','#ca8a04','#ea580c','#dc2626'];
    function getAvatarColor(u){let h=0;for(let i=0;i<(u||'u').length;i++)h+=(u||'u').charCodeAt(i);return avatarColors[h%avatarColors.length];}
    return d.comentarios.map(com=>{
      const uname = com.username||'usuario';
      const initials = uname.substring(0,2).toUpperCase();
      const color = getAvatarColor(uname);
      const igUrl = 'https://www.instagram.com/'+uname;
      const repliesHtml = com.replies&&com.replies.data&&com.replies.data.length
        ? com.replies.data.map(r=>{
            const rn=r.username||'lauro';
            return `<div style="display:flex;gap:8px;align-items:flex-start;margin-top:8px;margin-left:46px;background:#f8fafc;border-radius:8px;padding:8px 10px;border-left:3px solid #1a3d2b">
              <div class="com-avatar" style="width:28px;height:28px;font-size:10px;background:${getAvatarColor(rn)};flex-shrink:0">${rn.substring(0,2).toUpperCase()}</div>
              <div><a href="https://www.instagram.com/${rn}" target="_blank" style="font-weight:700;font-size:12px;color:#1a3d2b;text-decoration:none">@${rn}</a>
              <div style="font-size:12px;color:#374151;margin-top:2px">${r.text}</div>
              <div style="font-size:10px;color:#94a3b8;margin-top:2px">${new Date(r.timestamp).toLocaleString('pt-BR')}</div></div></div>`;
          }).join('') : '';
      return `<div class="com-item">
        <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px">
          <div style="display:flex;gap:10px;align-items:flex-start;flex:1;min-width:0">
            <a href="${igUrl}" target="_blank" title="Ver perfil no Instagram" style="flex-shrink:0">
              <div class="com-avatar" style="background:${color}">${initials}</div>
            </a>
            <div style="flex:1;min-width:0">
              <a href="${igUrl}" target="_blank" style="text-decoration:none">
                <div class="com-username" style="display:flex;align-items:center;gap:5px">
                  @${uname}
                  <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="#94a3b8" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
                </div>
              </a>
              <div class="com-texto">${com.text}</div>
              <div class="com-data">${new Date(com.timestamp).toLocaleString('pt-BR')}</div>
            </div>
          </div>
          <button onclick="mostrarResposta('${com.id}')" style="background:#f1f5f9;border:none;border-radius:6px;padding:5px 10px;cursor:pointer;font-size:12px;font-weight:600;white-space:nowrap;display:flex;align-items:center;gap:4px;flex-shrink:0">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
            Responder
          </button>
        </div>
        <div id="form-resposta-${com.id}" style="display:none;margin-top:8px;margin-left:48px">
          <textarea id="texto-resposta-${com.id}" placeholder="Digite sua resposta..." style="width:100%;padding:8px;border:1px solid #e2e8f0;border-radius:6px;font-size:12px;resize:vertical;min-height:50px;box-sizing:border-box"></textarea>
          <div style="display:flex;gap:6px;margin-top:6px">
            <button onclick="enviarRespostaComentario('${com.id}')" style="background:#1a3d2b;color:white;border:none;border-radius:6px;padding:6px 14px;cursor:pointer;font-size:12px;font-weight:600;display:flex;align-items:center;gap:4px">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg>
              Enviar
            </button>
            <button onclick="document.getElementById('form-resposta-${com.id}').style.display='none'" style="background:#f1f5f9;border:none;border-radius:6px;padding:6px 14px;cursor:pointer;font-size:12px">Cancelar</button>
          </div>
        </div>
        ${repliesHtml}
      </div>`;
    }).join('');
  })();
  }).catch(()=>{ document.getElementById('comentarios-lista').innerHTML='<div style="color:#ef4444;padding:16px">Erro ao carregar comentarios.</div>'; });
}

function mostrarResposta(comId) {
  const form = document.getElementById('form-resposta-'+comId);
  form.style.display = form.style.display === 'none' ? 'block' : 'none';
}

function fecharComentarios() {
  document.getElementById('modal-comentarios').classList.remove('ativo');
  igMediaAtual = null;
}

function enviarRespostaComentario(comId) {
  const texto = document.getElementById('texto-resposta-'+comId).value.trim();
  if(!texto) return;
  fetch('/api/instagram/comentarios/'+comId+'/responder', {
    method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({texto})
  }).then(r=>r.json()).then(d=>{
    if(d.ok) { document.getElementById('form-resposta-'+comId).style.display='none'; abrirComentarios(igMediaAtual); }
    else alert('Erro ao enviar resposta.');
  });
}

// Carrega feed quando clicar na aba Integracoes - integrado via evento
// ─── FIM INSTAGRAM JS ─────────────────────────────────────────────────────────

// ─── CANVA ────────────────────────────────────────────────────────────────────
var _canvaCarregado=false;
function canvaCarregarDesigns(){
  var grid=document.getElementById('canva-designs-grid');
  if(!grid) return;
  grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:20px;color:var(--texto2)">Carregando designs do Canva...</div>';
  fetch('/marketing/canva/designs').then(function(r){return r.json();}).then(function(d){
    if(!d.ok){grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:20px;color:#dc2626">'+(d.erro||'Erro ao carregar designs')+'</div>';return;}
    if(!d.designs.length){grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:20px;color:var(--texto2)">Nenhum design encontrado na conta Canva. Clique em "Criar novo design" para comecar.</div>';return;}
    grid.innerHTML=d.designs.map(function(des){
      var thumb=(des.thumbnail&&des.thumbnail.url)||'/img/placeholder.png';
      var titulo=des.title||'Sem titulo';
      var editUrl=des.urls&&des.urls.edit_url;
      return '<div class="midia-card">'
        +'<img src="'+thumb+'" alt="'+titulo+'" onerror="this.onerror=null;this.src=\'/img/placeholder.png\'">'
        +'<div class="midia-overlay" style="flex-direction:column;gap:6px;padding:8px">'
        +(editUrl?'<a href="'+editUrl+'" target="_blank" class="btn-xs" style="background:white;color:#111;text-decoration:none;width:100%;justify-content:center">Editar</a>':'')
        +'<button onclick="canvaImportar(\''+des.id+'\',\''+titulo.replace(/'/g,"\\'")+'\',this)" class="btn-xs" style="background:#0F6E56;color:white;border-color:transparent;width:100%;justify-content:center">Importar</button>'
        +'</div>'
        +'<div class="midia-card-info">'+titulo+'</div>'
        +'</div>';
    }).join('');
    _canvaCarregado=true;
  }).catch(function(){grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:20px;color:#dc2626">Erro ao carregar designs do Canva</div>';});
}
function previewWpp(texto){
  var p=document.getElementById('wpp-preview-text');
  if(p) p.textContent=texto||'Sua mensagem aparecera aqui...';
}
function usarTemplate(btn){
  var ta=document.getElementById('wpp-mensagem');
  if(!ta) return;
  ta.value=btn.textContent.trim();
  previewWpp(ta.value);
  ta.scrollIntoView({behavior:'smooth',block:'center'});
}
function canvaTogglePersonalizado(tipo){
  var mostrar=tipo==='Personalizado';
  document.getElementById('canva-largura').style.display=mostrar?'':'none';
  document.getElementById('canva-altura').style.display=mostrar?'':'none';
}
function canvaCriarDesign(){
  var tipo=document.getElementById('canva-tipo-novo').value;
  var largura=document.getElementById('canva-largura').value;
  var altura=document.getElementById('canva-altura').value;
  if(tipo==='Personalizado' && (!largura||!altura)){
    mostrarAviso('Informe largura e altura para o formato personalizado.','erro');
    return;
  }
  var win=window.open('','_blank'); // abre a aba na hora do clique, para o navegador nao bloquear como pop-up
  if(win) win.document.write('Abrindo o Canva...');
  fetch('/marketing/canva/criar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({tipo:tipo,largura:largura,altura:altura})})
    .then(function(r){return r.json();}).then(function(d){
      if(!d.ok){if(win)win.close(); mostrarAviso(d.erro||'Nao foi possivel criar o design.','erro');return;}
      if(d.editUrl && win) win.location.href=d.editUrl;
      else if(d.editUrl) window.open(d.editUrl,'_blank');
      setTimeout(canvaCarregarDesigns,1500);
    }).catch(function(){if(win)win.close(); mostrarAviso('Erro ao criar design no Canva.','erro');});
}
function canvaImportar(designId,titulo,btn){
  btn.disabled=true; btn.textContent='Importando...';
  fetch('/marketing/canva/importar/'+designId,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({nome:titulo})})
    .then(function(r){return r.json();}).then(function(d){
      if(!d.ok){mostrarAviso(d.erro||'Nao foi possivel importar.','erro'); btn.disabled=false; btn.textContent='Importar'; return;}
      mostrarAviso('Design importado para a biblioteca de midias!','sucesso');
      setTimeout(function(){location.reload();},1200);
    }).catch(function(){mostrarAviso('Erro ao importar design.','erro'); btn.disabled=false; btn.textContent='Importar';});
}
</script>
</body>
</html>